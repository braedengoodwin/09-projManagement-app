export default function AddProject({}) {

    function handleClick()
    {
        <input className="bg-blue"> test input </input>
    }

  return (
    <button className="w-36 h-12 bg-neutral-700 text-neutral-500 hover:bg-neutral-500 hover:text-white rounded-md text-lg font-semibold"
        onClick={handleClick}
    >
      + Add Project
    </button>
  );
}
